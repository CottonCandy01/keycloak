kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: keycloak
spec:
#  host: keycloak.apps-crc.testing
  subdomain: keycloak
  port:
    targetPort: http
  to:
    kind: Service
    name: keycloak
